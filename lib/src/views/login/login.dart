import 'package:flutter/material.dart';
import 'package:myevent/src/views/login/components/body.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
